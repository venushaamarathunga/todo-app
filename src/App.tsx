const App = () => {
  
  const handleAddTodo =() =>{

  }

  return(
  <div>
   <input type="text" name='todo' placeholder="add the todos..." />
   <button onClick={handleAddTodo}>Add</button>
  </div>
  )

}

export default App;